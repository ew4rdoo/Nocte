export default function FeedPage() {
  return (
    <main
      className="px-6 pt-16"
      style={{ paddingBottom: "calc(env(safe-area-inset-bottom, 0px) + 64px)" }}
    >
      <p className="font-sans text-nocte-muted text-sm">Feed coming soon.</p>
    </main>
  );
}
